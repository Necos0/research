# ロードマップ(中間発表まで)

- [x] 1 Medeasiのデータセット、`<FKGL>` タグを使った再現実験を行う
  - [x] 1.1 学習サイズを限りなく小さくし、研究室GPUでテスト
  - [x] 1.2 1Bのモデルで実際に動かす
- [ ] 2 Medeasiのデータセットに対して、`<keep>` タグで保持する情報を追加する
  - [x] 2.0 データにkeepタグのメトリックスを追加、プロンプトを追加
  - [x] 2.0.1 可読性フィルタなしのフルデータ（1892事例）でkeep付きスプリットを作成（`data/splits_flattened_full/medeasi`、runスクリプト内の`--local_data_dir`で指定）
  - [x] 2.1 学習サイズを限りなく小さくし、研究室GPUでテスト（スモーク設定は準備済み・サーバー実走行待ち）
  - [x] 2.2 1Bのモデルで実際に動かす（完了。数値あり47件で保持率 pred 0.674 / ref 0.617、SARI 40.25・LENS 58.53。結果は `results/keep-medeasi-1b/`）
- [ ] 3 結果を比較する（**プロンプト構築バグ発覚により 3.1〜3.2 は無効。再実行が必要**）
  - [x] 3.1 FKGLタグをKEEPと同一のフルデータで学習し、学習データの交絡を排除（結果は `results/fkgl-medeasi-1b-full/`。args差分はmetric_nameのみ＝クリーンな比較）
  - [x] 3.2 同一test 203件で「保持率・SARI/LENS・制御MAE」の比較表を作成（保持率0.560→0.674・品質同等・p≈0.065）。→ **3.3 の理由により無効と判断し、比較表 `docs/comparison_fkgl_full_vs_keep.md` は削除した**
  - [ ] 3.3 **【判明した重大な問題】** 3.1 の出力203件を目視した結果、平易化が成立していないことが判明。予測の 202/203 が `The ` で始まる定型崩壊（参照文は1割）、原文コピー11件、事実の捏造・非文33件、数値保持 0.560。原因は制御トークンが簡約文と別ターンに分離するプロンプト構築バグと、BOS を completion に混入させるトークン化バグ（FKGL・KEEP 共通の共有コード）。**SARI 40.66 は論文値と一致していたが品質を保証しておらず**、捏造事例が SARI 67.6 を記録していた。3.1/3.2 の結論（特に「品質同等」）は支持できない。
  - [x] 3.4 バグを修正して FKGL フルデータ版を再実行（ブランチ `exp/fkgl-medeasi-1b-full-v2`。修正3点は README「修正したバグ」節）。**定型崩壊は解消**：`The ` 始まりが 99.5%（202/203）→ **10.8%（22/203）** と参照文（10.3%）並みに回復。制御トークンの出力への漏れ 0件。SARI 51.24 / LENS 56.79 / FKGL MAE 2.92（v1 の SARI 40.66 より改善、論文水準）。BERTScore も backfill で復活（to_ref 0.905）。結果は `results/fkgl-medeasi-1b-full-v2/`
    - **ただし新たな懸念：平易化が弱い**。予測の **110/203（54.2%）が原文と完全一致**（参照文は 0/203＝必ず編集する）。FKGL も 原文14.11 → 予測12.74 と下げる方向には動くが参照文 11.88 には届かずアンダーシュート。空出力6件。破綻ではなく「保守的すぎる」モデル。early stopping が epoch 1.38（step 130）で止まっており under-fit 気味の可能性。
    - **重要：この結果で 3.2 の結論は完全に覆る**。FKGL ベースラインの数値保持率が **0.560 → 0.915**（参照文 0.617）に上がったため、旧 3.2 の「KEEP 優位（0.560→0.674）」は成立しない。KEEP 再実行後に改めて比較すること。原文コピーが多いほど数値保持率は自動的に上がるため、**比較では保持率と原文コピー率を必ずセットで見る**。
  - [ ] 3.5 同じ修正で KEEP 1B も再実行（ブランチ `exp/keep-medeasi-1b-v2`。`exp/keep-medeasi-1b` は同一バグを踏んでいる）。`exp/fkgl-medeasi-1b-full-v2` から分岐し、**差分は `METRIC_NAME=KEEP` のみ**＝ハイパラ・データ・修正コードは完全同一。修正後のプロンプト構築が KEEP でも正しく動くことは、実際の tokenizer でレンダリングして検証済み（単数値・複数値・`none` の3パターンとも制御トークンが簡約文の接頭辞になり、BOS 混入なし）。
  - [ ] 3.6 再実行後の健全な出力で比較表を作り直す（`docs/comparison_fkgl_full_vs_keep.md` を新規作成。作成前に必ず出力を目視し、`The ` 始まりの比率が参照文並みの約1割に戻っていることを確認する）。**比較表に必ず載せる列**: 数値保持率（数値あり47件）／**原文コピー率**／SARI・LENS／FKGL MAE／空出力数
